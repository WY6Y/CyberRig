fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pyserial>=3.5
websockets>=12.0
# Waterfall (FTDX10 USB audio FFT on the shack PC)
numpy>=1.24.0
sounddevice>=0.4.6
# PySide6>=6.6.0   <- ui_legacy only, not needed for web server
